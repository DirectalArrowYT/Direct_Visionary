# Workshop-local temporary data
/tmp/

# Rust build output
/target/

# Build outputs from projects consolidated into the monorepo
**/target/
**/__pycache__/
**/*.py[cod]
/research/decomp/ssbu-re/venv/

# ── Copyrighted / proprietary game assets (Smash Ultimate extracts) ─────────
# Never commit ripped shaders, effects, models, textures, or dump output.

# Accidental local input trees
/input/

# Accidental export-tree copies in the repo root
/export/
/effect/
/fighter/

# Local build / toolchain caches (must not be committed)
/.cache/
/.cargo-tmp/
/.git/

# Common Nintendo / Smash asset extensions (anywhere in this repo)
*.bnsh
*.bntx
*.nutexb
*.numatb
*.numshb
*.numdlb
*.nusktb
*.nuanmb
*.nufx
*.eff
*.ptcl
*.vfxb
*.bfres
*.msbf
*.arc
*.szs
*.prc
*.bin
*.nso
*.nca
*.nsp
*.xci
*.rdc
*.zip
*.nro

# Game-derived effect-name dictionaries are external runtime inputs.
/plugins/slight_replica/src/slight/effect_viewer/effect_names.txt

# Editor/IDE
.vscode/
.kiro/

rustc-ice-*.txt

# Raw research captures must remain outside the repository.
/research/hitbox-captures/

# SLight reference/decompilation is intentionally local-only. The only SLight
# implementation eligible for commit is plugins/slight_replica/.
/research/decomp/slight/
/legacy/
/tools/rust-parameter-manager/
/test-assets/

# Claude Code workspace settings
.claude/
visionary.exe - Shortcut.lnk
